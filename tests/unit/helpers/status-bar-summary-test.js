
import { statusBarSummary } from 'status-tracker-ember/helpers/status-bar-summary';
import { module, test } from 'qunit';

module('Unit | Helper | status bar summary');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = statusBarSummary([42]);
  assert.ok(result);
});

