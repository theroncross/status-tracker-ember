
import { statusBarTime } from 'status-tracker-ember/helpers/status-bar-time';
import { module, test } from 'qunit';

module('Unit | Helper | status bar time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = statusBarTime([42]);
  assert.ok(result);
});

