
import { statusBarProgress } from 'status-tracker-ember/helpers/status-bar-progress';
import { module, test } from 'qunit';

module('Unit | Helper | status bar progress');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = statusBarProgress([42]);
  assert.ok(result);
});

