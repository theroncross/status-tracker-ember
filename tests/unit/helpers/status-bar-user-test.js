
import { statusBarUser } from 'status-tracker-ember/helpers/status-bar-user';
import { module, test } from 'qunit';

module('Unit | Helper | status bar user');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = statusBarUser([42]);
  assert.ok(result);
});

