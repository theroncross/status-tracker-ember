
import { statusBarStatus } from 'status-tracker-ember/helpers/status-bar-status';
import { module, test } from 'qunit';

module('Unit | Helper | status bar status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = statusBarStatus([42]);
  assert.ok(result);
});

